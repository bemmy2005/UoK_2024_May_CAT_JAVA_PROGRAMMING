package Qwstion3;

 class Circle<lass> {
	     double length;
	     double base;
	   

	    public Circle(double length, double base, double height) {
	        this.length = length;
	        this.base = base;
	        
	    }

	    public double calculateSurfaceArea() {
	        return 2 * (length * base + length *  + base);
	    }

	    public double calculateperimeter() {
	        return length * base ;
	    }
	}

